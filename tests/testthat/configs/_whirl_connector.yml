metadata:
  path: !expr withr::local_tempdir()

datasources:
  - name: "folder"
    backend:
        type: "ConnectorFS"
        path: "{metadata.path}"
  - name: "database"
    backend:
        type: "ConnectorDBI"
        drv: "RSQLite::SQLite()"
        dbname: ":memory:"
